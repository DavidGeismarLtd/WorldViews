<div class="detailed-analysis">
  <% if interpretation.detailed_content.present? %>
    <div style="--persona-color: <%= persona.color_primary %>;">
      <%= sanitize(interpretation.detailed_content, tags: %w[h3 h4 p ul li strong em blockquote], attributes: %w[]) %>
    </div>
  <% else %>
    <div class="flex items-center justify-center py-12">
      <div class="text-center flex flex-col items-center">
        <!-- Brain thinking animation -->
        <div class="text-7xl animate-pulse mb-2" style="animation-duration: 1.5s;">
          🧠
        </div>
        <!-- Thinking dots (centered below brain) -->
        <div class="flex space-x-1 mb-6">
          <div class="w-2.5 h-2.5 rounded-full animate-bounce" style="background-color: <%= persona.color_primary %>; animation-delay: 0s;"></div>
          <div class="w-2.5 h-2.5 rounded-full animate-bounce" style="background-color: <%= persona.color_primary %>; animation-delay: 0.2s;"></div>
          <div class="w-2.5 h-2.5 rounded-full animate-bounce" style="background-color: <%= persona.color_primary %>; animation-delay: 0.4s;"></div>
        </div>
        <p class="text-gray-700 text-xl font-medium mb-2">
          <%= persona.name %> is crafting a detailed analysis...
        </p>
        <p class="text-gray-500 text-sm">
          Deep thinking in progress • This may take a minute
        </p>
      </div>
    </div>
  <% end %>
</div>
