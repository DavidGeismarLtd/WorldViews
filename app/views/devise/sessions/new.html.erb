<div class="min-h-screen bg-gradient-to-br from-gray-50 via-purple-50/30 to-indigo-50/30 py-12 relative overflow-hidden">
  <!-- Decorative background elements -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-20 left-10 text-6xl opacity-5 animate-float">🌍</div>
    <div class="absolute top-40 right-20 text-5xl opacity-5 animate-float-delayed">📰</div>
    <div class="absolute bottom-40 left-20 text-5xl opacity-5 animate-float">🎭</div>
    <div class="absolute bottom-20 right-10 text-6xl opacity-5 animate-float-delayed">✨</div>
  </div>

  <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <!-- Header -->
    <div class="text-center mb-8">
      <div class="text-6xl mb-4 animate-bounce-slow">👋🌍</div>
      <h1 class="text-4xl font-bold text-gray-900 mb-2">Welcome Back</h1>
      <p class="text-lg text-gray-600">
        Log in to manage your personas
      </p>
    </div>

    <!-- Form -->
    <div class="bg-white rounded-2xl shadow-xl p-10 sm:p-12 border-t-4 border-purple-600 backdrop-blur-sm">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="mb-8">
          <%= f.label :email, "Email", class: "block text-base font-semibold text-gray-700 mb-3" %>
          <div class="relative">
            <span class="absolute left-4 top-4 text-xl text-gray-400">📧</span>
            <%= f.email_field :email, autofocus: true, autocomplete: "email",
              class: "w-full pl-12 pr-4 py-4 text-lg border-2 border-gray-300 rounded-xl focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-200 hover:border-purple-300",
              placeholder: "you@example.com" %>
          </div>
        </div>

        <div class="mb-8">
          <%= f.label :password, "Password", class: "block text-base font-semibold text-gray-700 mb-3" %>
          <div class="relative">
            <span class="absolute left-4 top-4 text-xl text-gray-400">🔒</span>
            <%= f.password_field :password, autocomplete: "current-password",
              class: "w-full pl-12 pr-4 py-4 text-lg border-2 border-gray-300 rounded-xl focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-200 hover:border-purple-300",
              placeholder: "••••••••" %>
          </div>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="mb-8">
            <label class="flex items-center cursor-pointer group">
              <%= f.check_box :remember_me, class: "mr-3 w-5 h-5 text-purple-600 border-gray-300 rounded focus:ring-purple-500" %>
              <span class="text-base text-gray-700 group-hover:text-gray-900 transition-colors">Remember me</span>
            </label>
          </div>
        <% end %>

        <div class="mb-6">
          <%= f.submit "Log In 🚀", class: "w-full px-8 py-5 bg-gradient-to-r from-purple-600 to-indigo-600 text-white rounded-xl font-bold text-xl hover:shadow-2xl transform hover:scale-105 transition-all duration-200 cursor-pointer hover:from-purple-700 hover:to-indigo-700" %>
        </div>
      <% end %>

      <div class="text-center pt-4 border-t border-gray-200">
        <%= render "devise/shared/links" %>
      </div>
    </div>

    <!-- Additional info -->
    <p class="text-center text-xs text-gray-500 mt-4">
      🔐 Your data is secure and encrypted
    </p>
  </div>
</div>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
  }

  @keyframes float-delayed {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-15px); }
  }

  @keyframes bounce-slow {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }

  .animate-float-delayed {
    animation: float-delayed 8s ease-in-out infinite;
  }

  .animate-bounce-slow {
    animation: bounce-slow 3s ease-in-out infinite;
  }
</style>
