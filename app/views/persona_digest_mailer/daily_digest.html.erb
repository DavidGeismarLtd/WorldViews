<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      margin: 0;
      padding: 0;
      background-color: #f9fafb;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
    }
    .header {
      background: linear-gradient(135deg, <%= @persona.color_primary %> 0%, <%= @persona.color_secondary %> 100%);
      color: white;
      padding: 40px 30px;
      text-align: center;
    }
    .header h1 {
      margin: 0 0 10px 0;
      font-size: 28px;
      font-weight: bold;
    }
    .header p {
      margin: 0;
      font-size: 16px;
      opacity: 0.9;
    }
    .content {
      padding: 30px;
    }
    .greeting {
      font-size: 16px;
      margin-bottom: 20px;
    }
    .story {
      margin-bottom: 30px;
      padding: 20px;
      border-left: 4px solid <%= @persona.color_primary %>;
      background: #f9fafb;
      border-radius: 8px;
    }
    .story h3 {
      margin-top: 0;
      margin-bottom: 8px;
      color: <%= @persona.color_primary %>;
      font-size: 20px;
    }
    .story-meta {
      color: #666;
      font-size: 13px;
      margin-bottom: 12px;
    }
    .story-content {
      font-size: 15px;
      line-height: 1.6;
      margin-bottom: 15px;
    }
    .btn {
      display: inline-block;
      padding: 12px 24px;
      background: <%= @persona.color_primary %>;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      font-size: 14px;
    }
    .btn:hover {
      opacity: 0.9;
    }
    .empty-state {
      text-align: center;
      padding: 40px 20px;
      color: #666;
    }
    .footer {
      text-align: center;
      padding: 30px;
      background-color: #f9fafb;
      color: #666;
      font-size: 13px;
      line-height: 1.8;
    }
    .footer a {
      color: <%= @persona.color_primary %>;
      text-decoration: none;
    }
    .footer a:hover {
      text-decoration: underline;
    }
    .tagline {
      font-style: italic;
      margin-top: 15px;
      color: #999;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1><%= @persona.name %>'s Daily Takes</h1>
      <p><%= @date.strftime('%B %d, %Y') %></p>
    </div>

    <div class="content">
      <p class="greeting">Hi <%= @user.email.split('@').first.capitalize %>,</p>

      <p>Here are <%= @persona.name %>'s latest hot takes on today's news:</p>

      <% if @interpretations.any? %>
        <% @interpretations.each do |interpretation| %>
          <div class="story">
            <h3><%= interpretation.news_story.headline %></h3>
            <p class="story-meta">
              <%= interpretation.news_story.source %> •
              <%= interpretation.news_story.published_at.strftime('%B %d, %Y') %>
            </p>
            <div class="story-content">
              <%= simple_format(interpretation.content) %>
            </div>
            <%= link_to 'Read Full Story', news_story_url(interpretation.news_story), class: 'btn' %>
          </div>
        <% end %>
      <% else %>
        <div class="empty-state">
          <p>📭 No new takes today. Check back tomorrow!</p>
        </div>
      <% end %>
    </div>

    <div class="footer">
      <p>You're receiving this because you follow <%= @persona.name %> on WorldViews.</p>
      <p>
        <%= link_to 'Unfollow', persona_follow_url(@persona), method: :delete %> |
        <%= link_to 'View All Personas', personas_url %>
      </p>
      <p class="tagline">"The news, seen through everyone's delusions"</p>
    </div>
  </div>
</body>
</html>

