<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Worldviews" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Worldviews">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Header -->
    <header class="bg-white shadow-sm fixed top-0 left-0 right-0 z-50">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <a href="<%= root_path %>" class="flex items-center space-x-2">
            <div class="text-2xl">🌍</div>
            <div>
              <h1 class="text-xl font-bold text-gray-900">Worldviews</h1>
              <p class="text-xs text-gray-500 italic">The news, seen through everyone's delusions</p>
            </div>
          </a>

          <!-- Navigation -->
          <nav class="hidden sm:flex items-center space-x-6">
            <%= link_to root_path, class: "text-gray-700 hover:text-gray-900 font-medium transition-colors" do %>
              📰 Stories
            <% end %>
            <%= link_to personas_path, class: "text-gray-700 hover:text-gray-900 font-medium transition-colors" do %>
              👥 Personas
            <% end %>
            <% if user_signed_in? %>
              <%= link_to new_persona_path, class: "text-purple-600 hover:text-purple-700 font-semibold transition-colors" do %>
                ✨ Create Persona
              <% end %>
              <%= link_to destroy_user_session_path, method: :delete, class: "text-gray-700 hover:text-gray-900 font-medium transition-colors" do %>
                Logout
              <% end %>
            <% else %>
              <%= link_to new_user_session_path, class: "text-gray-700 hover:text-gray-900 font-medium transition-colors" do %>
                Login
              <% end %>
              <%= link_to new_user_registration_path, class: "bg-purple-600 text-white px-4 py-2 rounded-lg hover:bg-purple-700 font-medium transition-colors" do %>
                Sign Up
              <% end %>
            <% end %>
          </nav>

          <!-- Mobile Menu Button -->
          <button class="sm:hidden text-gray-700" onclick="document.getElementById('mobile-menu').classList.toggle('hidden')">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="hidden sm:hidden mt-4 pb-2 border-t border-gray-200 pt-4">
          <%= link_to root_path, class: "block py-2 text-gray-700 hover:text-gray-900 font-medium" do %>
            📰 Stories
          <% end %>
          <%= link_to personas_path, class: "block py-2 text-gray-700 hover:text-gray-900 font-medium" do %>
            👥 Personas
          <% end %>
          <% if user_signed_in? %>
            <%= link_to new_persona_path, class: "block py-2 text-purple-600 hover:text-purple-700 font-semibold" do %>
              ✨ Create Persona
            <% end %>
            <%= link_to destroy_user_session_path, method: :delete, class: "block py-2 text-gray-700 hover:text-gray-900 font-medium" do %>
              Logout
            <% end %>
          <% else %>
            <%= link_to new_user_session_path, class: "block py-2 text-gray-700 hover:text-gray-900 font-medium" do %>
              Login
            <% end %>
            <%= link_to new_user_registration_path, class: "block py-2 bg-purple-600 text-white px-4 rounded-lg hover:bg-purple-700 font-medium text-center" do %>
              Sign Up
            <% end %>
          <% end %>
        </div>
      </div>
    </header>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="container mx-auto px-4 mt-20">
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4" role="alert">
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="container mx-auto px-4 <%= notice.present? ? '' : 'mt-20' %>">
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4" role="alert">
          <%= alert %>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="container mx-auto px-4 mt-24 pb-12">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-gray-200 mt-12">
      <div class="container mx-auto px-4 py-6 text-center text-gray-600 text-sm">
        <p>Worldviews • A satirical media literacy experiment</p>
        <p class="mt-2 text-xs text-gray-500">All interpretations are AI-generated parodies. Don't take them seriously.</p>
      </div>
    </footer>
  </body>
</html>
